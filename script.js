function toggleInfo(id) {
    const section = document.getElementById(id);
    
    // Toggles the visibility class seamlessly with your premium CSS layout rules
    section.classList.toggle('is-visible');
}